{
  "name": "automate dashboards",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && ncc build lib/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@vercel/ncc": "^0.36.0"
  }
}
